//Libraries import
import { Router, Response, Request } from "express";

const router = Router();

// Get all products save in the database
export default router.get("/products", async (req: Request, res: Response) => {
  // Get products to be done here
});
